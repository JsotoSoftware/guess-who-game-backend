APP_ENV=dev
HTTP_ADDR=:8080

# PostgreSQL
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=database_name
POSTGRES_DSN=postgres://user:password@localhost:5432/database_name?sslmode=disable

# Redis
REDIS_ADDR=localhost:6379
REDIS_PORT=6379
REDIS_PASSWORD=password

# Anonymous Login
JWT_SECRET=your_secret_key
COOKIE_SECURE=false
COOKIE_DOMAIN=